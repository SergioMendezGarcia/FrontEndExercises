var fecha = new Date();

dia.innerHTML = fecha.getDate();

diaSemana.innerHTML = fecha.getDay();

año.innerHTML = fecha.getFullYear();

hora.innerHTML = fecha.getHours();

minutos.innerHTML = fecha.getMinutes();

segundos.innerHTML = fecha.getSeconds();